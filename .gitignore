bin/
obj/
.lscache
.DS_Store
node_modules/
e2e/node_modules/
e2e/playwright-report/
e2e/test-results/

# Scripts
reset-db.sh

# Local environment
.env

# SQLite database files
gasoholic.db
gasoholic.db-shm
gasoholic.db-wal

# Deployment secrets — generated by deploy.sh, never commit
.deploy-secrets

# Bicep compiled ARM JSON (generated artifact)
infra/main.json

# NONGRATIS
DOMAIN.md
DEPLOYMENT.md
tmp/
deploy.sh

# Angular build output — generated by `ng build` inside Docker.
# Do NOT commit these; CI always rebuilds from source.
# To run `dotnet run` locally, first: cd client && npm run build
wwwroot/browser/
wwwroot/prerendered-routes.json
wwwroot/3rdpartylicenses.txt
